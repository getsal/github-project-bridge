services:
  github-projects-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    image: github-projects-bridge:latest
    env_file:
      - path: .env
        required: false
    command: ["node", "dist/mcp.js"]
